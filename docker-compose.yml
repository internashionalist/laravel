services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - laravel_app:/var/www/html
    command: ["php", "artisan", "serve", "--host=0.0.0.0", "--port=8000"]
volumes:
  laravel_app:
